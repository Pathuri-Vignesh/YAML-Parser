# YAML-Parser
A custom YAML parser was created using Python that can parse configuration files and transform them into structured data. It can handle key-value pairs, nested objects, arrays, strings, numbers, booleans, and null values as well as multiline text blocks.
